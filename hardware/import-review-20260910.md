# PCB 工程迁移提交核对

日期：2026-09-10。目的：将原DGX机箱目录中的PCB设计完整迁入ESP32仓库，补入已有PR #2。
范围包括Rev A历史设计、Rev B原生工程和阶段快照、参考资料、脚本、制造发布与采购记录。
当前制造主包仍为 `fan-interface-revB/release-20260909-dfm5`，旧发布仅留作溯源。

## 隐私检查误报处置

此前本机钩子因主模型服务不可用，使用本地正则后备检测，拦截10个文本/审计文件、
282处account_number命中。自动建议会把数值替换成文本占位符，损坏JSON/EDA数据。

本轮人工检查检测逻辑和命中上下文，并把复核扩展到全部可解码的硬件文本，
包括钩子此前因大小限制跳过的EPRU文件。共找到1096处同类数字命中：

- 73处位于EDA的updateTime/version时间戳字段。
- 1023处位于几何数值的小数部分。
- 该后备检测器没有发现其他标签类型；以上不等于承诺任意检测器都零误报。

另检查工程文本中的凭据/私密收货字段、报价/DFM文档的可提取文字和相关装配截图；
未把账号收货页、发票资料或支付链接加入工程。已有Wi-Fi凭据仍被Git忽略。

用户在得知拦截原因后明确要求“提交上去啊”。本次在复核后使用钩子提供的
`PRIVACY_FILTER_SKIP=1`进程级例外完成这一条硬件导入提交；不写入仓库配置、
全局配置或shell环境，也不关闭其他Git钩子。后续提交恢复正常检查。
原生设计、制造ZIP、Gerber、BOM/CPL和冻结发布文件不为适配误报而修改。

## 验证与边界

- DFM5原始SHA256SUMS的48项均通过；提交后从Git树再次核对同一清单。
- Git空白检查通过；原导出中的尾部空行由.gitattributes作限定豁免，未改制造字节。
- 不修改固件、GB10服务或现有Wi-Fi配置；本次不需要固件构建/刷机。
- 采购状态沿用2026-09-09最后一次网页核实，本次没有刷新物流或确认生产稿。
- PCB生产稿、首板焊接和电气/固件联调仍待完成；Rev C issue #1保持后备。
